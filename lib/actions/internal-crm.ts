'use server';
import { auth } from '@/auth';
import { redirect } from 'next/navigation';
import { revalidatePath } from 'next/cache';
import { saveProspect, addCrmActivity } from '@/lib/internal-crm';
async function admin(){const s=await auth();if(!s)redirect('/login');if(!s.user.isPlatformAdmin)redirect('/dashboard')}
export async function saveProspectAction(formData:FormData){await admin();const id=String(formData.get('id')||'')||undefined,organizationName=String(formData.get('organizationName')||'').trim();if(!organizationName)redirect('/admin/crm?error=Organization+name+required');const arr=String(formData.get('estimatedArr')||'').trim();const p=await saveProspect({id,organizationName,contactName:String(formData.get('contactName')||'').trim()||null,email:String(formData.get('email')||'').trim()||null,phone:String(formData.get('phone')||'').trim()||null,website:String(formData.get('website')||'').trim()||null,source:String(formData.get('source')||'').trim()||null,stage:String(formData.get('stage')||'PROSPECT'),owner:String(formData.get('owner')||'').trim()||null,estimatedArrCents:arr?Math.round(Number(arr)*100):null,nextAction:String(formData.get('nextAction')||'').trim()||null,nextActionDate:String(formData.get('nextActionDate')||'').trim()||null,notes:String(formData.get('notes')||'').trim()||null});await addCrmActivity(p.id,id?'UPDATE':'CREATED',id?'Prospect record updated':'Prospect created');revalidatePath('/admin/crm');redirect('/admin/crm?success=Prospect+saved')}
export async function addCrmActivityAction(formData:FormData){await admin();const prospectId=String(formData.get('prospectId')||''),note=String(formData.get('note')||'').trim(),activityType=String(formData.get('activityType')||'NOTE');if(prospectId&&note)await addCrmActivity(prospectId,activityType,note);revalidatePath('/admin/crm');redirect('/admin/crm?success=Activity+added')}
